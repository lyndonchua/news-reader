# News Article Reader

Reader-only version of the Firebase News Article Compiler.

## What it does
- Reads articles from the existing `newsArticles` Firestore collection.
- Does not add, edit or delete articles.
- Includes search, source/date filters, tag filtering, dark mode, summary toggle and font size controls.

## Deploy
Upload this folder to GitHub and deploy on Vercel as a static app.
